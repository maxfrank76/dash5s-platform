# Flask
SECRET_KEY=your-secret-key-here-change-in-production
FLASK_DEBUG=True

# Database
DATABASE_URL=sqlite:///dash5s.db

# LDAP / Active Directory
LDAP_SERVER=localhost
LDAP_PORT=389
LDAP_USE_SSL=false
LDAP_BASE_DN=dc=test,dc=local
LDAP_USER_DN=ou=users,dc=test,dc=local
LDAP_GROUP_DN=ou=groups,dc=test,dc=local
LDAP_USER_LOGIN_ATTR=mail
LDAP_ADMIN_GROUP=cn=Dash5S_Admins,ou=groups,dc=test,dc=local
LDAP_EDITOR_GROUP=cn=Dash5S_Editors,ou=groups,dc=test,dc=local

# Optional: если требуется bind-пользователь для поиска
# LDAP_BIND_DN=cn=admin,dc=test,dc=local
# LDAP_BIND_PASSWORD=admin